let a = 5 + (2 - 10) * 3;

console.log(a);

/**
 * (>) Mayor que
 * (<) Menor que
 * (>=) Mayor o igual que
 * (<=) Menor o igual que
 * (=) Operador de asignación (asignación)
 * (==) Comparación de valor
 * (===) Comparación de tipo de dato y de valor     
 * (!) Negación
 * (||) OR
 * (&&) AND
*/

let i = 6;
// i += 3;

console.log(`El resultado de sumar 3 a el valor de ${i} es:`, i += 3);

console.log(`El resultado de restar 2 a ${i} es:`, i -= 2);


// Imprime el resultado y después suma 1 unidad
console.log(i++);
console.log(i);